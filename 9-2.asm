;Write a program that rotate the word stored at 
;0100:0300h memory address to left until it will 
;be negative and save the rotation count in CX. 
;If the last value is bigger than signed value A100h 
;then write AAh to 0100:1000h, if not write 
;BBh to 0100:1000h.